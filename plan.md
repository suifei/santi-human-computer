# 三体·人列计算机 3D 模拟器 — 执行计划

## 目标
浏览器可打开的 3D 交互模拟器：约 1000 名秦朝士兵组成方阵，红/蓝旗 = 1/0，
实现真实门级电路仿真（AND/OR/XOR/NOT → 全加器 → 行波加法器 + 移位乘法器），
默认演示 `(A+B)×C`（如 (1013+1012)×1001=2027025）。古代战场氛围：夯土地面、
门牌号地格、火把、战鼓、薄雾。Three.js 渲染，InstancedMesh 保证性能。

## Stage 1 — 技能加载与架构设计（主 Agent）
- 读取 `vibecoding-webapp-swarm` SKILL.md，确定交付方式（预览/版本卡）
- 设计电路编译器：十进制输入 → 二进制 → 门级网表（拓扑分层）→ 士兵坐标映射
- 输出：netlist 设计 + 场景布局设计，传给 Stage 2

## Stage 2 — 编码实现（coder 子代理，按 swarm-workspace 规范）
子代理接收：① 本计划 ② 电路编译器设计 ③ 视觉规范（低饱和暖色调、秦朝风）
- 场景：地面网格+门牌号、火把/鼓台/王座（监军台）、雾效、天空
- 士兵：InstancedMesh 低多边形秦兵（甲胄锥形身+头盔+旗杆），红旗/蓝旗翻转动画
- 仿真：鼓点节拍驱动，按层传播；可播放/暂停/单步/调速
- UI：A、B、C 十进制输入，二进制显示，结果十进制+二进制输出；点击士兵弹指令卡
  （"门牌号：613，功能：XOR，上游：529/607"）；相机环绕/飞行
- 输出：可构建的 Vite+React（或按技能模板）项目

## Stage 3 — 验证与交付
- 构建成功、电路真值校验（多组随机 A/B/C 对照 JS 原生计算）
- website_version_manager build_version 交付版本卡
