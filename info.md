# 研究资料：三体"人列计算机"原理（来自用户上传文档）

## 背景
《三体》小说中，秦始皇用三千万士兵组成"人列计算机"。本模拟器以**秦朝士兵**方阵复刻：
每个士兵 = 一个二输入逻辑门（AND/OR/XOR/NOT），举**红旗=1、蓝旗=0**，
每名士兵只盯两名指定上游士兵的旗色，听统一鼓点节拍同时举旗，信号逐层传递，
最终完成二进制算术运算。

## 核心规则
- 信息：红=1，蓝=0；鼓点统一节拍，鼓响瞬间所有人同时读写，举旗保持到下一拍
- 节点：1 人 = 1 个逻辑门；每人直视两个指定上游
- 每人持指令卡："门牌号：613，功能：XOR，上游 A：529，上游 B：607，真值表：红蓝→红，其余→蓝"
- 复位：连续 3 快拍 → 全员强制蓝旗（全局 0）
- DONE 旗举红 → 运算结束，读取输出

## 全加器 FA（5 门，4 层）
- L1: 输入 A、B、Cin
- L2: XOR1(A,B)、AND1(A,B)
- L3: XOR2(XOR1,Cin)→Sum、AND2(XOR1,Cin)
- L4: OR(AND1,AND2)→Cout

## 位宽扩展
- n-bit 行波加法器：纵向复制 FA，横向串进位；每 FA 内 4 层，不同位同层并行
- 乘法：移位相加（部分积 AND + 加法器阵列）
- 减法：XOR 取反 + Cin=1（补码）

## 演示目标
默认演示 `(A+B)×C`，示例 (1013+1012)×1001 = 2027025。
十进制输入 → 二进制 → 沿方阵 X/Y 轴注入 → 人列计算 → 输出端读出结果。
士兵规模约 1000 人（门+输入+输出+寄存器士兵）。

## 视觉要求（秦朝/古代风格）
- 秦兵：深色甲胄、皮甲/青铜色调、发髻、戈戟、双面红蓝大旗
- 场景：夯土/黄土操场、1m×1m 地格贴门牌号、战鼓台、监军台、火把、薄雾、黄昏暖光
- 低饱和暖色调、宏大史诗感；击鼓节拍视觉/听觉反馈
